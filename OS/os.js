import { homedir } from 'os';
console.log(homedir());

import { hostname } from "os";
console.log(hostname());

import { type } from "os";
console.log(type());

import { version } from "os";
console.log(version());